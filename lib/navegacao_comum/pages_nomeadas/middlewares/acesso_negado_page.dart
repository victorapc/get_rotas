import 'package:flutter/material.dart';

class AcessoNegadoPage extends StatelessWidget {
  const AcessoNegadoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Acesso Negado'),
      ),
      body: Container(),
    );
  }
}
