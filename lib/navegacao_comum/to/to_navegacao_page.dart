import 'package:flutter/material.dart';

class ToNavegacaoPage extends StatelessWidget {
  const ToNavegacaoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page de Navegação'),
      ),
      body: Container(),
    );
  }
}
