import 'package:flutter/material.dart';

class OffNavegacaoPage3 extends StatelessWidget {
  const OffNavegacaoPage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Page de Navegação 3'),
      ),
      body: Container(),
    );
  }
}
