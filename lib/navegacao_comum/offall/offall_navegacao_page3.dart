import 'package:flutter/material.dart';

class OffallNavegacaoPage3 extends StatelessWidget {
  const OffallNavegacaoPage3({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Navegacao Page 3'),
      ),
      body: Container(),
    );
  }
}
